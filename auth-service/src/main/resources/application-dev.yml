privacy:
  encryption:
    key: ${PRIVACY_ENCRYPTION_KEY:DevPrivacyKey123}  # 16자리 AES-128, 로컬 개발용 기본값

management:
  tracing:
    sampling:
      probability: 1.0
logging:
  level:
    root: DEBUG
    com.onmeet: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
