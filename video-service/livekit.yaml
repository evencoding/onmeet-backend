port: 7880
rtc:
  port_range_start: 50000
  port_range_end: 50100
  tcp_port: 7881
  use_external_ip: true

# API keys and Webhook are passed via environment variables in docker-compose.yml
keys: {}

logging:
  level: info
