server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/onmeet?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: onmeet
    password: "1234"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true


